**Keywords**
- reinforcement learning, deep reinforcement learning, contextual bandits, policy optimization
- dynamic cache replacement, cache eviction, cache admission, cache management, cache indexing
- LRU, LFU, ARC, Belady, learned caching, adaptive caching
- retrieval-augmented generation, RAG, LLM serving, inference cache, prefix cache, KV cache, semantic cache
- workload drift, non-stationary workload, latency optimization, hit rate, throughput, tail latency, cost reduction
- multi-tenant serving, online learning, offline RL, production systems

**Boolean search queries**
- `("reinforcement learning" OR "deep reinforcement learning" OR RL OR "contextual bandit") AND ("cache replacement" OR "cache eviction" OR "cache admission" OR caching) AND (LLM OR "large language model" OR RAG OR "retrieval-augmented generation")`
- `("learned caching" OR "adaptive caching" OR "dynamic cache replacement") AND (inference OR serving OR backend OR "production system") AND (language model OR transformer OR RAG)`
- `("KV cache" OR "prefix cache" OR "semantic cache") AND (reinforcement learning OR policy learning OR bandit) AND (latency OR throughput OR hit rate)`
- `("cache management" AND "non-stationary workload") AND (RL OR "reinforcement learning") AND ("LLM serving" OR RAG)`
- `(LRU OR LFU OR ARC OR Belady) AND ("reinforcement learning" OR learned OR adaptive) AND (cache OR caching) AND (LLM OR RAG)`